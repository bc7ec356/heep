# HEEP - Requirements
# ===================
# Core dependencies for training, inference, and data curation

# Core
numpy>=1.20.0
torch>=2.0.0
torchaudio>=2.0.0

# HuggingFace ecosystem
transformers>=4.36.0
accelerate>=0.25.0
datasets>=2.14.0
huggingface-hub>=0.20.0
safetensors>=0.4.0

# Audio processing
soundfile>=0.12.0
librosa>=0.9.0

# Tokenization
sentencepiece>=0.1.99
tiktoken>=0.5.0

# Training utilities
tensorboard>=2.14.0

# Optional: Grapheme-to-phoneme conversion
g2p_en>=2.1.0

# Optional: Flash Attention 2 (install separately)
# pip install flash-attn --no-build-isolation

# Development/Testing
pytest>=7.0.0
